{
  "name": "sleeper-draft-mcp",
  "version": "0.1.0",
  "description": "Local MCP server for live visibility into a Sleeper fantasy football draft, using Sleeper's public keyless REST API.",
  "type": "module",
  "bin": {
    "sleeper-draft-mcp": "dist/server.js"
  },
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "node --experimental-strip-types src/server.ts",
    "test": "node --experimental-strip-types --test \"test/**/*.test.ts\" && node web-tests/logic.mjs && node web-tests/espn.mjs && node web-tests/render.mjs"
  },
  "keywords": [
    "mcp",
    "sleeper",
    "fantasy-football",
    "draft"
  ],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.19.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "jsdom": "^29.1.1",
    "typescript": "^5.7.3"
  }
}
